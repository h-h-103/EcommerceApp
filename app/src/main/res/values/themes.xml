<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcommerceApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/g_black</item>
        <item name="colorPrimaryVariant">@color/g_black</item>
    </style>

    <style name="Theme.EcommerceApp" parent="Base.Theme.EcommerceApp" />

    <style name="screenBackground">
        <item name="android:background">@color/g_white</item>
    </style>

    <style name="cardBackground">
        <item name="android:background">@color/g_white</item>
    </style>

    <style name="textColor">
        <item name="android:textColor">@color/g_black</item>
    </style>

</resources>